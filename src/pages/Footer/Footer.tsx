import React from 'react';
import Dropdown from '../../components/Dropdown';

export const Footer: React.FC = () => {

    return (
        <div className="footer">
            <button className='create'>Create Plan</button>
        </div>
    );
}